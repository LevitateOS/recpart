# recpart frontend

The interactive TUI frontend has been removed.

Use the backend directly:

```bash
recpart plan --disk <disk> --mode ab --json
recpart apply --disk <disk> --mode ab --dry-run --json
```
